'use client'
/**
 * `@status experimental` の部品の `'use client'` 版（ADR-0009）。semver の対象外。
 * stable の部品は `@rimltempest/riml-ds-react/client` から出る。
 */
export * from './generated/client/experimental.js'
