/**
 * `@rimltempest/riml-ds-elements/experimental/carousel`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./carousel` に移し、このパスを 1 メジャー残す。
 */
export * from '../../carousel/index.js'
