/**
 * `@rimltempest/riml-ds-elements/experimental/select`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./select` に移し、このパスを 1 メジャー残す。
 */
export * from '../../select/index.js'
