/**
 * `@rimltempest/riml-ds-elements/experimental/data-table`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./data-table` に移し、このパスを 1 メジャー残す。
 */
export * from '../../data-table/index.js'
