/**
 * `@rimltempest/riml-ds-elements/experimental/popover`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./popover` に移し、このパスを 1 メジャー残す。
 */
export * from '../../popover/index.js'
