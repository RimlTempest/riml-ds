/**
 * `@rimltempest/riml-ds-elements/experimental/number-field`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./number-field` に移し、このパスを 1 メジャー残す。
 */
export * from '../../number-field/index.js'
