/**
 * `@rimltempest/riml-ds-elements/experimental/tabs`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./tabs` に移し、このパスを 1 メジャー残す。
 */
export * from '../../tabs/index.js'
