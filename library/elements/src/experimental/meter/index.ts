/**
 * `@rimltempest/riml-ds-elements/experimental/meter`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./meter` に移し、このパスを 1 メジャー残す。
 */
export * from '../../meter/index.js'
