/**
 * `@rimltempest/riml-ds-elements/experimental/slider`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./slider` に移し、このパスを 1 メジャー残す。
 */
export * from '../../slider/index.js'
