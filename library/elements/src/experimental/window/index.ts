/**
 * `@rimltempest/riml-ds-elements/experimental/window`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./window` に移し、このパスを 1 メジャー残す。
 */
export * from '../../window/index.js'
