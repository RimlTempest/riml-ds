/**
 * `@rimltempest/riml-ds-elements/experimental/radio-group`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./radio-group` に移し、このパスを 1 メジャー残す。
 */
export * from '../../radio-group/index.js'
