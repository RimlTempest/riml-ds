/**
 * `@rimltempest/riml-ds-elements/experimental/combobox`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./combobox` に移し、このパスを 1 メジャー残す。
 */
export * from '../../combobox/index.js'
