/**
 * `@rimltempest/riml-ds-elements/experimental/splitter`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./splitter` に移し、このパスを 1 メジャー残す。
 */
export * from '../../splitter/index.js'
