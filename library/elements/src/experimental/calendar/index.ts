/**
 * `@rimltempest/riml-ds-elements/experimental/calendar`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./calendar` に移し、このパスを 1 メジャー残す。
 */
export * from '../../calendar/index.js'
