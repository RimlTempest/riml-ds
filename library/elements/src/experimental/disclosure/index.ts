/**
 * `@rimltempest/riml-ds-elements/experimental/disclosure`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./disclosure` に移し、このパスを 1 メジャー残す。
 */
export * from '../../disclosure/index.js'
