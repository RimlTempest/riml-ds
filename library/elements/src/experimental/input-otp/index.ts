/**
 * `@rimltempest/riml-ds-elements/experimental/input-otp`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./input-otp` に移し、このパスを 1 メジャー残す。
 */
export * from '../../input-otp/index.js'
