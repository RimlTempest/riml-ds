/**
 * `@rimltempest/riml-ds-elements/experimental/tooltip`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./tooltip` に移し、このパスを 1 メジャー残す。
 */
export * from '../../tooltip/index.js'
