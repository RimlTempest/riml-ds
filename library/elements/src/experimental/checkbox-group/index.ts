/**
 * `@rimltempest/riml-ds-elements/experimental/checkbox-group`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./checkbox-group` に移し、このパスを 1 メジャー残す。
 */
export * from '../../checkbox-group/index.js'
