/**
 * `@rimltempest/riml-ds-elements/experimental/command`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./command` に移し、このパスを 1 メジャー残す。
 */
export * from '../../command/index.js'
