/**
 * `@rimltempest/riml-ds-elements/experimental/toggle-group`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./toggle-group` に移し、このパスを 1 メジャー残す。
 */
export * from '../../toggle-group/index.js'
