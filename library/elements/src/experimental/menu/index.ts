/**
 * `@rimltempest/riml-ds-elements/experimental/menu`。
 * `@status experimental` の部品はここからだけ export する（ADR-0009）。
 * stable に上げるときは `exports` を `./menu` に移し、このパスを 1 メジャー残す。
 */
export * from '../../menu/index.js'
